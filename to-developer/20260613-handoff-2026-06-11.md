# Handoff: coding CLI → Codex Session

> 时间: 2026-06-11
> 来源: coding CLI (Claude) session
> 目标: Codex session 接管

## 项目背景

ARIS (Auto-research-in-sleep) 框架，从上游魔改起步，当前重点是 **framework / project / dev 三层分离** + **安装器契约硬化**。本 session 由 coding CLI 完成大部分结构清理和回归测试，用户计划切到 Codex session 继续。

---

## 当前进度

### 已完成（本次 session）

| 工作 | 路径 | 说明 |
|------|------|------|
| dev framework 骨架 | `aris-dev/*/` | 12 个核心目录 + README |
| dev 开发文档区 | `aris-dev/to-developer/` | `plans/` + `logs/` + `deploy-QAs/` + `discussions/` |
| CHANGELOG | `aris-orangmood-edition/CHANGELOG.md` | 从 git history 提取 |
| Promote 流程文档 | `aris-dev/to-developer/plans/20260613-PROMOTE_FLOW.md` | 手动流程 |
| 仓库演进计划 | `aris-dev/to-developer/plans/20260613-REPO_EVOLUTION_PLAN.md` | 分仓时机、promote 工具规划 |
| 框架结构契约 | `aris-orangmood-edition/docs/FRAMEWORK_STRUCTURE.md` | 三层边界 + dev framework 说明 |
| `--dev` 标志 | `aris-orangmood-edition/tools/install_aris.sh` | 自动检测同级/父级 `aris-dev/` |
| `--reconcile` 测试 | `tests/test_install_aris_reconcile.py` | 4 tests（添加/删除/拒绝/幂等） |
| `--Codex` 测试 | `tests/test_install_aris_codex_flag.py` | 5 tests |
| manifest 安全测试 | `tests/test_install_aris_manifest_safety.py` | 5 tests |
| version record 测试 | `tests/test_install_aris_version_record.py` | 4 tests |
| dev flag 测试 | `tests/test_install_aris_dev_flag.py` | 3 tests |
| tools symlink 测试 | `tests/test_install_aris_tools_symlink.py` | 7 tests |

**总计: 28 个 installer 回归测试全部通过。**

### 已提交

commit 已打，分支 `orangmood-tripartite-night-20260512`，包含上述所有变更。

---

## 重要文档索引

| 文档 | 路径 | 用途 |
|------|------|------|
| 框架结构契约 | `docs/FRAMEWORK_STRUCTURE.md` | framework/project/dev 边界定义 |
| Promote 流程 | `aris-dev/to-developer/plans/20260613-PROMOTE_FLOW.md` | dev → stable 手动流程 |
| 仓库演进计划 | `aris-dev/to-developer/plans/20260613-REPO_EVOLUTION_PLAN.md` | 分仓时机、promote 工具规划 |
| CHANGELOG | `CHANGELOG.md` | 版本变更记录 |
| 原始 plan | `~/.claude/plans/pure-nibbling-hippo.md` | grill-with-docs 阶段的完整 plan |

---

## 已知问题 / 技术债

1. **已有测试失败**: `test_watchdog.py` 等 64 个 pre-existing errors（与本次改动无关，Bytes vs str 问题）。如需要修复，另开任务。
2. **重复全局 skill**: `~/.claude/skills/paper-illustration-image2/` 仍存在，与框架内 skill 重复。清理待做。
3. **dev 还是空的**: `aris-dev/skills/` 等目录只有 README，无实际实验内容。
4. **promote 未自动化**: 只有文档和手动检查清单，无 `check_promote_ready.sh` 脚本。
5. **`assets/` 分类未定**: `aris-orangmood-edition/assets/` 里的图片是框架展示用，已保留，但需确认是否全部属于 framework。

---

## 之后计划（用户确认的候选）

按优先级排序：

1. **修复已有测试错误** — `test_watchdog.py` 等 64 个 pre-existing failures
2. **框架功能开发** — 新 skill、现有工具优化
3. **部署相关** — 3090x2 服务器 Docker 配置（历史上下文，用户之前提过）
4. **开源准备** — 用户曾提"转开源框架对agent做更多约束"，但明确 deferred

用户本次 session 最后确认的核心方向：**"仓库问题 + promote 策略"**，已写入 `20260613-REPO_EVOLUTION_PLAN.md`，结论是**保持同仓、延后分仓、promote 工具等第一次真正 promote 时再做**。

---

## 建议 Skills（Codex session）

| Skill | 何时用 |
|-------|--------|
| `/tdd` | 继续为安装器或其他模块写回归测试 |
| `/diagnose` | 修复 `test_watchdog.py` 等已有测试错误 |
| `/grill-with-docs` | 如果要重新审视框架结构决策 |
| `/leader` | 如果要启动实验 pipeline（非当前重点） |
| `/caveman` | 用户说"caveman 模式"时开启，压缩通信 |

---

## 环境信息

- **Stable repo**: `/root/Projects/aris/Auto-research-in-sleep/aris-orangmood-edition/`
- **Dev repo**: `/root/Projects/aris/Auto-research-in-sleep/aris-dev/`
- **Framework (空)**: `/root/Projects/aris/Auto-research-in-sleep/aris-framework/`
- **Branch**: `orangmood-tripartite-night-20260512`
- **Python**: `python3` 可用，`python` 指向 python2（不要用）

---

*本 handoff 由 coding CLI (Claude) 生成，供 Codex session 接管时阅读。*
