# ARIS Single-User GPU Deployment
# For deploying ARIS with full GPU access on a single machine.
#
# Usage:
#   1. Copy deploy/ dir to GPU server
#   2. Copy .env.gpu.example → .env, fill in values
#   3. docker compose -f docker-compose.gpu.yaml up -d
#   4. docker exec -it aris-gpu bash

services:
  aris:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.gpu
      args:
        USERNAME: ${USERNAME:-researcher}
        UID: ${USER_UID:-1000}
        GID: ${USER_GID:-1000}
        ARIS_BRANCH: ${ARIS_BRANCH:-orangmood-tripartite-night-20260512}
        BUILD_HTTP_PROXY: ${HTTP_PROXY:-}
        BUILD_HTTPS_PROXY: ${HTTPS_PROXY:-}
    container_name: aris-gpu
    hostname: aris-gpu
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - ANTHROPIC_BASE_URL=${ANTHROPIC_BASE_URL:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - HTTP_PROXY=${HTTP_PROXY:-}
      - HTTPS_PROXY=${HTTPS_PROXY:-}
      - NO_PROXY=${NO_PROXY:-}
      - http_proxy=${http_proxy:-}
      - https_proxy=${https_proxy:-}
      - no_proxy=${no_proxy:-}
      - GIT_HTTP_PROXY=${GIT_HTTP_PROXY:-}
      - GIT_HTTPS_PROXY=${GIT_HTTPS_PROXY:-}
      - ARIS_AUTO_CHECK_UPDATE=${ARIS_AUTO_CHECK_UPDATE:-1}
      - ARIS_UPDATE_CHECK_INTERVAL=${ARIS_UPDATE_CHECK_INTERVAL:-1d}
      - ARIS_UPDATE_CHECK_TIMEOUT=${ARIS_UPDATE_CHECK_TIMEOUT:-10s}
      - FEISHU_APP_ID=${FEISHU_APP_ID:-}
      - FEISHU_APP_SECRET=${FEISHU_APP_SECRET:-}
      - FEISHU_USER_ID=${FEISHU_USER_ID:-}
      - FEISHU_RECEIVE_ID_TYPE=${FEISHU_RECEIVE_ID_TYPE:-open_id}
      - FEISHU_ENABLE_WS=${FEISHU_ENABLE_WS:-0}
      - BRIDGE_PORT=${BRIDGE_PORT:-5000}
      - ARIS_PROJECT_ROOT=${ARIS_PROJECT_ROOT:-/aris/framework}
      - ARIS_FEISHU_CONTROL_ROOT=${ARIS_FEISHU_CONTROL_ROOT:-}
      - NVIDIA_VISIBLE_DEVICES=${NVIDIA_VISIBLE_DEVICES:-all}
      - CUDA_VISIBLE_DEVICES=${CUDA_VISIBLE_DEVICES:-}
    volumes:
      # Framework checkout used by install_aris.sh and agent skills.
      - ${FRAMEWORK_PATH:?set FRAMEWORK_PATH in .env}:/aris/framework
      # Optional dev framework checkout; install_aris.sh --dev discovers /aris/aris-dev.
      - ${DEV_FRAMEWORK_PATH:?set DEV_FRAMEWORK_PATH in .env}:/aris/aris-dev
      # Project directory (your exp0516 etc.)
      - ${PROJECT_PATH:?set PROJECT_PATH in .env}:/aris/projects
      # Datasets (optional, read-only)
      - ${DATASETS_PATH:?set DATASETS_PATH in .env}:/aris/shared/datasets:ro
      # Pretrained models cache
      - ${PRETRAINED_PATH:?set PRETRAINED_PATH in .env}:/aris/shared/pretrained
      # SSH keys from host
      - ${SSH_PATH:?set SSH_PATH in .env}:/run/secrets/ssh:ro
      # Persist claude/codex config across rebuilds
      - claude-config:/home/${USERNAME:-researcher}/.claude
      - codex-config:/home/${USERNAME:-researcher}/.codex
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    stdin_open: true
    tty: true
    restart: unless-stopped
    # Keep container running
    command: ["bash"]

volumes:
  pretrained-cache:
  claude-config:
  codex-config:
